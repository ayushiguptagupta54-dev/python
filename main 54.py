#  Is vs == in python
a = [1,2,3]
b = [1,2,3]
c = (1,5,6)
d = (4,7,8)
e = "Ayush"
f = "Shivam"
g = None
h = None
print(a is b) # exact location of objects in memory
print(a == b) # exact location of objects in memory
print(c is d) # exact location of objects in memory
print(c == d) # exact location of objects in memory
print(e is f) # exact location of objects in memory
print(e == f) # exact location of objects in memory
print(g is h) # exact location of objects in memory
print(g == h) #exact location of objects in memory
